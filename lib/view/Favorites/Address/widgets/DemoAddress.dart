
import 'UserAddresses.dart';

class Address {
  final UserAddresses addresses;
  Address({
    required this.addresses,
});
}
List<Address> DemoAddress = [
  Address(addresses: useraddress[0]),
  Address(addresses: useraddress[1])
];